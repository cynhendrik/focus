interface Props { customerId: string }
export function ActivitiesPane({ customerId }: Props) {
  return <div style={{ padding: '20px 24px', color: 'var(--fg-dim)', fontSize: 12 }}>Activities werden geladen…</div>
}
