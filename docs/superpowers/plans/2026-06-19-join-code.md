# Beitritts-Code Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein zweiter Nutzer kann einem Workspace per Code beitreten (Owner zeigt/rotiert den Code, Member gibt ihn ein), wodurch der Workspace `isShared` wird und die bestehenden Cloud-Gateways + Realtime greifen.

**Architecture:** Eine `SECURITY DEFINER`-RPC `join_workspace_by_code` legt die Mitgliedschaft an (umgeht RLS gezielt, da der Beitretende den Workspace noch nicht lesen darf). Der `workspace.store` bekommt `generateJoinCode` (rein), `joinWorkspaceByCode`, `regenerateJoinCode` und ein `join_code`-Feld. Zwei kleine, isoliert testbare UI-Komponenten (`JoinCodeRow` für Owner, `JoinWorkspaceForm` für Member) hängen sich in die bestehenden `WorkspaceSettings`/`WorkspacePicker`.

**Tech Stack:** TypeScript, React 18, Zustand, Supabase (Postgres + RPC + RLS), Vitest, @testing-library/react.

**Spec:** `docs/superpowers/specs/2026-06-19-join-code-design.md`

---

## File Structure

| Datei | Verantwortung | Aktion |
|-------|---------------|--------|
| `supabase/migrations/0002_join_code.sql` | DB-Vertrag: Spalte + Backfill + RPC + RLS (manuell im Dashboard angewendet) | Create |
| `src/store/workspace.store.ts` | `generateJoinCode`, `join_code`-Feld, `joinWorkspaceByCode`, `regenerateJoinCode` | Modify |
| `src/store/workspace.store.test.ts` | Tests für die neuen Store-Teile | Modify |
| `src/test/setup.js` | globalen `supabase`-Mock um `rpc` + `auth.getUser` erweitern | Modify |
| `src/core/workspace/JoinCodeRow.tsx` | Owner-UI: Code anzeigen + neu generieren | Create |
| `src/core/workspace/JoinCodeRow.test.tsx` | Test | Create |
| `src/core/workspace/JoinWorkspaceForm.tsx` | Member-UI: Code eingeben + beitreten | Create |
| `src/core/workspace/JoinWorkspaceForm.test.tsx` | Test | Create |
| `src/components/settings/WorkspaceSettings.tsx` | `<JoinCodeRow/>` einhängen | Modify |
| `src/core/workspace/WorkspacePicker.tsx` | `<JoinWorkspaceForm/>` einhängen | Modify |

---

## Task 1: SQL-Migration (manueller Dashboard-Schritt)

**Files:**
- Create: `supabase/migrations/0002_join_code.sql`

Diese Datei ist der DB-Vertrag. Sie ist **nicht** lokal testbar (kein Postgres) und wird vom Menschen im Supabase-SQL-Editor angewendet (Projekt `mqbjmquscjtytpjebosw`). Keine TDD — Datei schreiben, committen, im Report klar als manuellen Schritt ausweisen.

- [ ] **Step 1: Migrationsdatei anlegen**

`supabase/migrations/0002_join_code.sql`:

```sql
-- 0002_join_code.sql — Beitritts-Code für Workspaces (Shared-Workspace Pilot, Subsystem B)
-- MANUELL im Supabase SQL-Editor anwenden (Projekt mqbjmquscjtytpjebosw).
-- Annahme aus 0001: workspace_members.workspace_id ist TEXT, workspaces.id ist uuid.
-- Falls das reale Schema abweicht, die ::text-Casts unten anpassen.

-- 1. Spalte
alter table public.workspaces add column if not exists join_code text unique;

-- 2. Backfill: bestehende Workspaces ohne Code bekommen einen 6-stelligen Zufallscode
--    aus dem sicheren Alphabet (ohne 0/O/1/I).
do $$
declare
  w record;
  alphabet text := 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
  code text;
  i int;
begin
  for w in select id from public.workspaces where join_code is null loop
    loop
      code := '';
      for i in 1..6 loop
        code := code || substr(alphabet, 1 + floor(random() * length(alphabet))::int, 1);
      end loop;
      begin
        update public.workspaces set join_code = code where id = w.id;
        exit;
      exception when unique_violation then
        -- Kollision: neu würfeln
      end;
    end loop;
  end loop;
end $$;

-- 3. RPC: per Code beitreten. SECURITY DEFINER, damit der (noch nicht berechtigte)
--    Beitretende den Workspace per Code finden und seine Mitgliedschaft anlegen kann.
create or replace function public.join_workspace_by_code(p_code text)
returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
  ws_id uuid;
begin
  select id into ws_id from public.workspaces where join_code = p_code;
  if ws_id is null then
    raise exception 'Ungültiger Code';
  end if;
  insert into public.workspace_members (workspace_id, user_id, role)
  values (ws_id::text, auth.uid(), 'member')
  on conflict do nothing;
  return ws_id;
end $$;

grant execute on function public.join_workspace_by_code(text) to authenticated;

-- 4. RLS: Owner darf workspaces (u.a. join_code) aktualisieren ("neu generieren").
alter table public.workspaces enable row level security;
drop policy if exists ws_update_owner on public.workspaces;
create policy ws_update_owner on public.workspaces
  for update
  using (
    id::text in (
      select workspace_id from public.workspace_members
      where user_id = auth.uid() and role = 'owner'
    )
  );
```

- [ ] **Step 2: Commit**

```bash
git add supabase/migrations/0002_join_code.sql
git commit -m "feat(db): join_code column + join_workspace_by_code RPC + owner update RLS"
```

- [ ] **Step 3: Im Report ausweisen**

Report enthält den Hinweis: „Manueller Schritt für Hendrik: `0002_join_code.sql` im Supabase-SQL-Editor (Projekt mqbjmquscjtytpjebosw) ausführen; danach prüfen, dass `join_code` auf `workspaces` existiert und `select public.join_workspace_by_code('TESTXX')` `Ungültiger Code` wirft."

---

## Task 2: `generateJoinCode` (reine Funktion, TDD)

**Files:**
- Modify: `src/store/workspace.store.ts`
- Test: `src/store/workspace.store.test.ts`

- [ ] **Step 1: Failing test ergänzen**

In `src/store/workspace.store.test.ts` den Import erweitern und einen Block anhängen. Import-Zeile (oben) ändern zu:

```ts
import { useWorkspaceStore, deriveShared, generateJoinCode } from './workspace.store'
```

Am Dateiende anfügen:

```ts
describe('generateJoinCode', () => {
  it('liefert 6 Zeichen aus dem sicheren Alphabet (ohne 0/O/1/I)', () => {
    for (let n = 0; n < 50; n++) {
      const code = generateJoinCode()
      expect(code).toMatch(/^[A-HJ-NP-Z2-9]{6}$/)
    }
  })
})
```

- [ ] **Step 2: Test laufen lassen, Fehlschlag bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: FAIL — `generateJoinCode` ist kein Export.

- [ ] **Step 3: Implementieren**

In `src/store/workspace.store.ts` direkt nach der `deriveShared`-Funktion (nach deren schließender `}`) einfügen:

```ts
const JOIN_CODE_ALPHABET = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'

/** Kurzer, verbal teilbarer Beitritts-Code: 6 Zeichen, ohne Verwechsler (0/O/1/I). */
export function generateJoinCode(length = 6): string {
  let out = ''
  for (let i = 0; i < length; i++) {
    out += JOIN_CODE_ALPHABET[Math.floor(Math.random() * JOIN_CODE_ALPHABET.length)]
  }
  return out
}
```

- [ ] **Step 4: Test laufen lassen, Erfolg bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: PASS (alle bestehenden + der neue Test).

- [ ] **Step 5: Commit**

```bash
git add src/store/workspace.store.ts src/store/workspace.store.test.ts
git commit -m "feat(workspace): generateJoinCode helper"
```

---

## Task 3: Test-Mock erweitern (`rpc` + `getUser`)

**Files:**
- Modify: `src/test/setup.js`

Der globale `supabase`-Mock hat heute kein `rpc` und kein `auth.getUser`. Beide brauchen die folgenden Tasks. Reine Test-Infrastruktur (kein Produktcode) → kein eigener Test, aber die volle Suite muss danach grün bleiben.

- [ ] **Step 1: Mock erweitern**

In `src/test/setup.js` den `@/lib/supabase`-Mock ersetzen durch:

```js
vi.mock('@/lib/supabase', () => ({
  supabase: {
    auth: {
      getSession: vi.fn().mockResolvedValue({ data: { session: null }, error: null }),
      getUser: vi.fn().mockResolvedValue({ data: { user: null }, error: null }),
      signInWithPassword: vi.fn().mockResolvedValue({ data: {}, error: null }),
      signOut: vi.fn().mockResolvedValue({ error: null }),
      onAuthStateChange: vi.fn().mockReturnValue({ data: { subscription: { unsubscribe: vi.fn() } } }),
    },
    from: vi.fn(() => ({
      select: vi.fn().mockReturnThis(),
      insert: vi.fn().mockReturnThis(),
      single: vi.fn().mockResolvedValue({ data: null, error: null }),
    })),
    rpc: vi.fn().mockResolvedValue({ data: null, error: null }),
  },
}))
```

- [ ] **Step 2: Volle Suite laufen lassen**

Run: `npm run test:run`
Expected: unverändert grün (nur `getUser`/`rpc` ergänzt, nichts entfernt).

- [ ] **Step 3: Commit**

```bash
git add src/test/setup.js
git commit -m "test: add rpc + auth.getUser to supabase mock"
```

---

## Task 4: `joinWorkspaceByCode` (Store-Action, TDD)

**Files:**
- Modify: `src/store/workspace.store.ts`
- Test: `src/store/workspace.store.test.ts`

- [ ] **Step 1: Failing test ergänzen**

Am Ende von `src/store/workspace.store.test.ts` anfügen:

```ts
describe('joinWorkspaceByCode', () => {
  it('ruft die RPC, lädt neu und aktiviert den beigetretenen Workspace', async () => {
    const { supabase } = await import('@/lib/supabase')
    vi.mocked(supabase.rpc).mockResolvedValueOnce({ data: 'ws-joined', error: null } as any)
    // loadWorkspaces läuft im Anschluss: ohne Session früher Ausstieg (Default-Mock).
    vi.mocked(supabase.auth.getSession).mockResolvedValueOnce({ data: { session: null }, error: null } as any)

    await useWorkspaceStore.getState().joinWorkspaceByCode('abc234')

    expect(vi.mocked(supabase.rpc)).toHaveBeenCalledWith('join_workspace_by_code', { p_code: 'ABC234' })
    expect(useWorkspaceStore.getState().activeWorkspaceId).toBe('ws-joined')
  })

  it('wirft eine lesbare Fehlermeldung bei ungültigem Code', async () => {
    const { supabase } = await import('@/lib/supabase')
    vi.mocked(supabase.rpc).mockResolvedValueOnce({ data: null, error: { message: 'Ungültiger Code' } } as any)
    await expect(useWorkspaceStore.getState().joinWorkspaceByCode('zzz999'))
      .rejects.toThrow('Ungültiger Code')
  })
})
```

Hinweis: Der Test erwartet, dass die Action den Code **getrimmt und großgeschrieben** an die RPC gibt (`'abc234'` → `'ABC234'`).

- [ ] **Step 2: Test laufen lassen, Fehlschlag bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: FAIL — `joinWorkspaceByCode` existiert nicht.

- [ ] **Step 3: Implementieren**

In `src/store/workspace.store.ts` die `WorkspaceState`-Schnittstelle erweitern (nach `createWorkspace`):

```ts
  createWorkspace: (name: string) => Promise<void>
  joinWorkspaceByCode: (code: string) => Promise<void>
```

In der Store-Implementierung nach `createWorkspace: async (name) => { … },` ergänzen:

```ts
      joinWorkspaceByCode: async (code) => {
        const { data, error } = await supabase.rpc('join_workspace_by_code', {
          p_code: code.trim().toUpperCase(),
        })
        if (error) throw new Error(error.message || 'Beitritt fehlgeschlagen')
        await get().loadWorkspaces()
        if (typeof data === 'string') set({ activeWorkspaceId: data })
      },
```

- [ ] **Step 4: Test laufen lassen, Erfolg bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/store/workspace.store.ts src/store/workspace.store.test.ts
git commit -m "feat(workspace): joinWorkspaceByCode action"
```

---

## Task 5: `join_code`-Feld + `createWorkspace` generiert Code (TDD)

**Files:**
- Modify: `src/store/workspace.store.ts`
- Test: `src/store/workspace.store.test.ts`

- [ ] **Step 1: Failing test ergänzen**

Am Ende von `src/store/workspace.store.test.ts` anfügen:

```ts
describe('createWorkspace', () => {
  it('schreibt einen generierten join_code mit', async () => {
    const { supabase } = await import('@/lib/supabase')
    vi.mocked(supabase.auth.getUser).mockResolvedValueOnce({ data: { user: { id: 'u1' } }, error: null } as any)
    // loadWorkspaces danach: keine Session → früher Ausstieg, keine weiteren from()-Aufrufe.
    vi.mocked(supabase.auth.getSession).mockResolvedValueOnce({ data: { session: null }, error: null } as any)

    let captured: any = null
    vi.mocked(supabase.from)
      .mockReturnValueOnce({
        insert: vi.fn((row: any) => { captured = row; return {
          select: vi.fn().mockReturnValue({
            single: vi.fn().mockResolvedValue({ data: { id: 'ws-new' }, error: null }),
          }),
        } }),
      } as any)
      .mockReturnValueOnce({
        insert: vi.fn().mockResolvedValue({ error: null }),
      } as any)

    await useWorkspaceStore.getState().createWorkspace('Neue Agentur')

    expect(captured.name).toBe('Neue Agentur')
    expect(captured.join_code).toMatch(/^[A-HJ-NP-Z2-9]{6}$/)
    expect(useWorkspaceStore.getState().activeWorkspaceId).toBe('ws-new')
  })
})
```

- [ ] **Step 2: Test laufen lassen, Fehlschlag bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: FAIL — `captured.join_code` ist `undefined`.

- [ ] **Step 3: Implementieren**

In `src/store/workspace.store.ts`:

(a) `Workspace`-Interface um `join_code` erweitern:

```ts
export interface Workspace {
  id: string
  name: string
  logo_url: string | null
  role: 'owner' | 'member'
  isShared: boolean
  join_code: string | null
}
```

(b) Im `loadWorkspaces`-Select `join_code` mitladen — die Zeile

```ts
          .select('workspace_id, role, workspaces(id, name, logo_url)')
```

ersetzen durch

```ts
          .select('workspace_id, role, workspaces(id, name, logo_url, join_code)')
```

(c) Im `base`-Mapping innerhalb `loadWorkspaces` `join_code` durchreichen — den Block

```ts
        const base = (data ?? []).map((m: any) => ({
          id: m.workspaces.id,
          name: m.workspaces.name,
          logo_url: m.workspaces.logo_url,
          role: m.role as 'owner' | 'member',
        }))
```

ersetzen durch

```ts
        const base = (data ?? []).map((m: any) => ({
          id: m.workspaces.id,
          name: m.workspaces.name,
          logo_url: m.workspaces.logo_url,
          role: m.role as 'owner' | 'member',
          join_code: m.workspaces.join_code ?? null,
        }))
```

(d) In `createWorkspace` den `insert` um `join_code` erweitern — die Zeile

```ts
          .insert({ name, created_by: user.id })
```

ersetzen durch

```ts
          .insert({ name, created_by: user.id, join_code: generateJoinCode() })
```

- [ ] **Step 4: Test laufen lassen, Erfolg bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: PASS. (Der bestehende `loadWorkspaces`-Test bleibt grün: `join_code` wird mit `?? null` toleriert.)

- [ ] **Step 5: Typecheck**

Run: `npm run typecheck`
Expected: keine Fehler. (Falls anderswo `Workspace`-Objekte literal erzeugt werden — z. B. im `loadWorkspaces`-„clears"-Test —, fügt der Test bereits `isShared` etc.; `join_code` ist optionalisiert über `?? null` nur im Mapping, im Interface aber `string | null` Pflicht. Der vorhandene Test in `workspace.store.test.ts:100` erzeugt ein Workspace-Literal **ohne** `join_code` → dort `join_code: null` ergänzen.)

Konkret: in `src/store/workspace.store.test.ts` die Zeile

```ts
    useWorkspaceStore.setState({ workspaces: [{ id: 'x', name: 'X', logo_url: null, role: 'owner', isShared: false }] })
```

ersetzen durch

```ts
    useWorkspaceStore.setState({ workspaces: [{ id: 'x', name: 'X', logo_url: null, role: 'owner', isShared: false, join_code: null }] })
```

- [ ] **Step 6: Test + Typecheck erneut**

Run: `npm run test:run -- src/store/workspace.store.test.ts && npm run typecheck`
Expected: beide grün.

- [ ] **Step 7: Commit**

```bash
git add src/store/workspace.store.ts src/store/workspace.store.test.ts
git commit -m "feat(workspace): join_code field + generate on createWorkspace"
```

---

## Task 6: `regenerateJoinCode` (Store-Action, TDD)

**Files:**
- Modify: `src/store/workspace.store.ts`
- Test: `src/store/workspace.store.test.ts`

- [ ] **Step 1: Failing test ergänzen**

Am Ende von `src/store/workspace.store.test.ts` anfügen:

```ts
describe('regenerateJoinCode', () => {
  it('setzt einen neuen Code per Update und aktualisiert den lokalen State', async () => {
    const { supabase } = await import('@/lib/supabase')
    useWorkspaceStore.setState({
      workspaces: [{ id: 'ws1', name: 'A', logo_url: null, role: 'owner', isShared: false, join_code: 'OLD234' }],
      activeWorkspaceId: 'ws1',
    })
    const eq = vi.fn().mockResolvedValue({ error: null })
    const update = vi.fn().mockReturnValue({ eq })
    vi.mocked(supabase.from).mockReturnValueOnce({ update } as any)

    const code = await useWorkspaceStore.getState().regenerateJoinCode('ws1')

    expect(code).toMatch(/^[A-HJ-NP-Z2-9]{6}$/)
    expect(update).toHaveBeenCalledWith({ join_code: code })
    expect(eq).toHaveBeenCalledWith('id', 'ws1')
    expect(useWorkspaceStore.getState().workspaces[0].join_code).toBe(code)
  })

  it('würfelt bei Unique-Kollision genau einmal neu', async () => {
    const { supabase } = await import('@/lib/supabase')
    useWorkspaceStore.setState({
      workspaces: [{ id: 'ws1', name: 'A', logo_url: null, role: 'owner', isShared: false, join_code: 'OLD234' }],
      activeWorkspaceId: 'ws1',
    })
    const eqFail = vi.fn().mockResolvedValue({ error: { code: '23505', message: 'duplicate' } })
    const eqOk   = vi.fn().mockResolvedValue({ error: null })
    vi.mocked(supabase.from)
      .mockReturnValueOnce({ update: vi.fn().mockReturnValue({ eq: eqFail }) } as any)
      .mockReturnValueOnce({ update: vi.fn().mockReturnValue({ eq: eqOk }) } as any)

    const code = await useWorkspaceStore.getState().regenerateJoinCode('ws1')
    expect(code).toMatch(/^[A-HJ-NP-Z2-9]{6}$/)
    expect(eqFail).toHaveBeenCalledTimes(1)
    expect(eqOk).toHaveBeenCalledTimes(1)
  })
})
```

- [ ] **Step 2: Test laufen lassen, Fehlschlag bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: FAIL — `regenerateJoinCode` existiert nicht.

- [ ] **Step 3: Implementieren**

In `src/store/workspace.store.ts` die `WorkspaceState`-Schnittstelle erweitern (nach `joinWorkspaceByCode`):

```ts
  joinWorkspaceByCode: (code: string) => Promise<void>
  regenerateJoinCode: (workspaceId: string) => Promise<string>
```

In der Store-Implementierung nach `joinWorkspaceByCode: async (code) => { … },` ergänzen:

```ts
      regenerateJoinCode: async (workspaceId) => {
        // Einmaliger Retry bei Unique-Kollision auf join_code.
        for (let attempt = 0; attempt < 2; attempt++) {
          const code = generateJoinCode()
          const { error } = await supabase
            .from('workspaces')
            .update({ join_code: code })
            .eq('id', workspaceId)
          if (!error) {
            set((s) => ({
              workspaces: s.workspaces.map((w) =>
                w.id === workspaceId ? { ...w, join_code: code } : w),
            }))
            return code
          }
          if (error.code !== '23505') throw new Error(error.message || 'Code-Aktualisierung fehlgeschlagen')
        }
        throw new Error('Konnte keinen eindeutigen Code erzeugen')
      },
```

- [ ] **Step 4: Test laufen lassen, Erfolg bestätigen**

Run: `npm run test:run -- src/store/workspace.store.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/store/workspace.store.ts src/store/workspace.store.test.ts
git commit -m "feat(workspace): regenerateJoinCode action"
```

---

## Task 7: `JoinCodeRow` (Owner-UI, TDD) + Einhängen

**Files:**
- Create: `src/core/workspace/JoinCodeRow.tsx`
- Test: `src/core/workspace/JoinCodeRow.test.tsx`
- Modify: `src/components/settings/WorkspaceSettings.tsx`

- [ ] **Step 1: Failing test schreiben**

`src/core/workspace/JoinCodeRow.test.tsx`:

```tsx
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest'
import { render, screen, fireEvent, cleanup } from '@testing-library/react'
import { JoinCodeRow } from './JoinCodeRow'
import { useWorkspaceStore } from '@/store/workspace.store'

afterEach(cleanup)
beforeEach(() => {
  useWorkspaceStore.setState({
    workspaces: [{ id: 'ws1', name: 'A', logo_url: null, role: 'owner', isShared: false, join_code: 'ABC234' }],
    activeWorkspaceId: 'ws1',
  })
})

describe('JoinCodeRow', () => {
  it('zeigt dem Owner den Code', () => {
    render(<JoinCodeRow />)
    expect(screen.getByText('ABC234')).toBeTruthy()
  })

  it('rendert nichts für Mitglieder (nicht-Owner)', () => {
    useWorkspaceStore.setState({
      workspaces: [{ id: 'ws1', name: 'A', logo_url: null, role: 'member', isShared: true, join_code: 'ABC234' }],
      activeWorkspaceId: 'ws1',
    })
    const { container } = render(<JoinCodeRow />)
    expect(container.firstChild).toBeNull()
  })

  it('„Neu generieren" ruft regenerateJoinCode mit der aktiven Workspace-Id', () => {
    const regenerate = vi.fn().mockResolvedValue('NEW234')
    useWorkspaceStore.setState({ regenerateJoinCode: regenerate })
    render(<JoinCodeRow />)
    fireEvent.click(screen.getByRole('button', { name: /neu generieren/i }))
    expect(regenerate).toHaveBeenCalledWith('ws1')
  })
})
```

- [ ] **Step 2: Test laufen lassen, Fehlschlag bestätigen**

Run: `npm run test:run -- src/core/workspace/JoinCodeRow.test.tsx`
Expected: FAIL — `./JoinCodeRow` nicht auflösbar.

- [ ] **Step 3: Implementieren**

`src/core/workspace/JoinCodeRow.tsx`:

```tsx
import { useState } from 'react'
import { Check, Copy, RefreshCw } from 'lucide-react'
import { useWorkspaceStore } from '@/store/workspace.store'

/** Owner-Ansicht: Beitritts-Code anzeigen, kopieren, neu generieren. */
export function JoinCodeRow() {
  const workspaces = useWorkspaceStore(s => s.workspaces)
  const activeId   = useWorkspaceStore(s => s.activeWorkspaceId)
  const regenerate = useWorkspaceStore(s => s.regenerateJoinCode)
  const ws = workspaces.find(w => w.id === activeId)
  const [copied, setCopied] = useState(false)
  const [busy, setBusy] = useState(false)

  if (!ws || ws.role !== 'owner') return null

  const code = ws.join_code ?? '—'

  const copy = () => {
    if (ws.join_code) {
      navigator.clipboard.writeText(ws.join_code)
      setCopied(true)
      setTimeout(() => setCopied(false), 1800)
    }
  }
  const onRegenerate = async () => {
    if (!activeId) return
    setBusy(true)
    try { await regenerate(activeId) } finally { setBusy(false) }
  }

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 5 }}>
      <label style={{ fontSize: 11, fontWeight: 600, color: 'var(--fg-muted)', letterSpacing: '0.04em', textTransform: 'uppercase' }}>
        Beitritts-Code
      </label>
      <div style={{ display: 'flex', gap: 6, alignItems: 'center' }}>
        <span style={{
          flex: 1, padding: '8px 12px', fontSize: 14, fontWeight: 700, letterSpacing: '0.12em',
          borderRadius: 8, border: '1px solid var(--border)', background: 'var(--surface-2)',
          color: 'var(--fg)', fontFamily: 'var(--font-mono)',
        }}>{code}</span>
        <button onClick={copy} aria-label="Code kopieren" style={btn}>
          {copied ? <Check size={14} style={{ color: 'var(--ok)' }} /> : <Copy size={14} />}
        </button>
        <button onClick={onRegenerate} disabled={busy} style={btn} title="Neu generieren">
          <RefreshCw size={14} />
          <span style={{ fontSize: 12 }}>Neu generieren</span>
        </button>
      </div>
      <p style={{ fontSize: 11, color: 'var(--fg-dim)', margin: 0 }}>
        Teile diesen Code, damit jemand deinem Workspace beitreten kann.
      </p>
    </div>
  )
}

const btn: React.CSSProperties = {
  padding: '0 10px', height: 34, borderRadius: 8, border: '1px solid var(--border)',
  background: 'var(--surface-2)', cursor: 'pointer', color: 'var(--fg-muted)',
  display: 'flex', alignItems: 'center', gap: 6,
}
```

- [ ] **Step 4: Test laufen lassen, Erfolg bestätigen**

Run: `npm run test:run -- src/core/workspace/JoinCodeRow.test.tsx`
Expected: PASS (3 Tests).

- [ ] **Step 5: In WorkspaceSettings einhängen**

In `src/components/settings/WorkspaceSettings.tsx` den Import ergänzen (bei den oberen Imports):

```tsx
import { JoinCodeRow } from '@/core/workspace/JoinCodeRow'
```

Und im JSX die `CopyField`-Zeile für die Workspace-ID

```tsx
        <div style={{ padding: '16px 20px' }}>
          <CopyField label="Workspace ID" value={workspaceId} />
        </div>
```

ersetzen durch

```tsx
        <div style={{ padding: '16px 20px', display: 'flex', flexDirection: 'column', gap: 16 }}>
          <CopyField label="Workspace ID" value={workspaceId} />
          <JoinCodeRow />
        </div>
```

- [ ] **Step 6: Typecheck**

Run: `npm run typecheck`
Expected: keine Fehler.

- [ ] **Step 7: Commit**

```bash
git add src/core/workspace/JoinCodeRow.tsx src/core/workspace/JoinCodeRow.test.tsx src/components/settings/WorkspaceSettings.tsx
git commit -m "feat(workspace): owner join-code row in settings"
```

---

## Task 8: `JoinWorkspaceForm` (Member-UI, TDD) + Einhängen

**Files:**
- Create: `src/core/workspace/JoinWorkspaceForm.tsx`
- Test: `src/core/workspace/JoinWorkspaceForm.test.tsx`
- Modify: `src/core/workspace/WorkspacePicker.tsx`

- [ ] **Step 1: Failing test schreiben**

`src/core/workspace/JoinWorkspaceForm.test.tsx`:

```tsx
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest'
import { render, screen, fireEvent, cleanup, waitFor } from '@testing-library/react'
import { JoinWorkspaceForm } from './JoinWorkspaceForm'
import { useWorkspaceStore } from '@/store/workspace.store'

afterEach(cleanup)

describe('JoinWorkspaceForm', () => {
  it('ruft joinWorkspaceByCode mit dem eingegebenen Code', async () => {
    const join = vi.fn().mockResolvedValue(undefined)
    useWorkspaceStore.setState({ joinWorkspaceByCode: join })
    render(<JoinWorkspaceForm />)
    fireEvent.change(screen.getByPlaceholderText(/code/i), { target: { value: 'abc234' } })
    fireEvent.click(screen.getByRole('button', { name: /beitreten/i }))
    await waitFor(() => expect(join).toHaveBeenCalledWith('abc234'))
  })

  it('zeigt eine Fehlermeldung bei ungültigem Code', async () => {
    const join = vi.fn().mockRejectedValue(new Error('Ungültiger Code'))
    useWorkspaceStore.setState({ joinWorkspaceByCode: join })
    render(<JoinWorkspaceForm />)
    fireEvent.change(screen.getByPlaceholderText(/code/i), { target: { value: 'zzz999' } })
    fireEvent.click(screen.getByRole('button', { name: /beitreten/i }))
    expect(await screen.findByText(/ungültiger code/i)).toBeTruthy()
  })
})
```

- [ ] **Step 2: Test laufen lassen, Fehlschlag bestätigen**

Run: `npm run test:run -- src/core/workspace/JoinWorkspaceForm.test.tsx`
Expected: FAIL — `./JoinWorkspaceForm` nicht auflösbar.

- [ ] **Step 3: Implementieren**

`src/core/workspace/JoinWorkspaceForm.tsx`:

```tsx
import { useState } from 'react'
import { useWorkspaceStore } from '@/store/workspace.store'

/** Member-Ansicht: einem Workspace per Beitritts-Code beitreten. */
export function JoinWorkspaceForm() {
  const joinWorkspaceByCode = useWorkspaceStore(s => s.joinWorkspaceByCode)
  const [code, setCode] = useState('')
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const handleJoin = async (e: React.FormEvent) => {
    e.preventDefault()
    if (!code.trim()) return
    setBusy(true)
    setError(null)
    try {
      await joinWorkspaceByCode(code)
    } catch (err: any) {
      setError(err?.message ?? 'Beitritt fehlgeschlagen')
    } finally {
      setBusy(false)
    }
  }

  return (
    <form onSubmit={handleJoin} className="flex flex-col gap-3">
      <p className="text-xs text-[var(--text2)] font-medium">Einem Workspace beitreten</p>
      <div className="flex gap-2">
        <input
          type="text"
          value={code}
          onChange={e => setCode(e.target.value)}
          placeholder="Beitritts-Code"
          className="flex-1 px-3 py-2.5 rounded-xl bg-[var(--bg1)] border border-[var(--border)] text-sm text-[var(--text)] focus:outline-none focus:border-primary placeholder-[var(--text2)]"
        />
        <button
          type="submit"
          disabled={busy || !code.trim()}
          className="px-4 py-2.5 rounded-xl bg-primary text-white text-sm font-semibold hover:bg-primary/90 transition-colors disabled:opacity-50"
        >
          {busy ? '…' : 'Beitreten'}
        </button>
      </div>
      {error && <p className="text-xs text-red-400">{error}</p>}
    </form>
  )
}
```

- [ ] **Step 4: Test laufen lassen, Erfolg bestätigen**

Run: `npm run test:run -- src/core/workspace/JoinWorkspaceForm.test.tsx`
Expected: PASS (2 Tests).

- [ ] **Step 5: In WorkspacePicker einhängen**

In `src/core/workspace/WorkspacePicker.tsx` den Import ergänzen (bei den oberen Imports):

```tsx
import { JoinWorkspaceForm } from './JoinWorkspaceForm'
```

Und direkt nach dem schließenden `</form>` des Erstellen-Formulars (vor dem „Ausloggen"-Button) einfügen:

```tsx
        <div className="mt-6 pt-6 border-t border-[var(--border)]">
          <JoinWorkspaceForm />
        </div>
```

- [ ] **Step 6: Typecheck + volle Suite**

Run: `npm run typecheck && npm run test:run`
Expected: Typecheck sauber; alle Tests grün.

- [ ] **Step 7: Commit**

```bash
git add src/core/workspace/JoinWorkspaceForm.tsx src/core/workspace/JoinWorkspaceForm.test.tsx src/core/workspace/WorkspacePicker.tsx
git commit -m "feat(workspace): member join-by-code form in picker"
```

---

## Abschluss-Verifikation (manuell, nach allen Tasks)

Erfordert das angewendete SQL + ein zweites Supabase-Konto:

1. `0002_join_code.sql` im Supabase-Dashboard ausführen (Projekt mqbjmquscjtytpjebosw).
2. Owner: Einstellungen öffnen → Beitritts-Code wird angezeigt; „Neu generieren" ändert ihn.
3. Zweites Konto: einloggen → im WorkspacePicker den Code eingeben → „Beitreten" → Workspace wird aktiv.
4. In Supabase prüfen: neue `workspace_members`-Zeile (role=member). Owner nach `loadWorkspaces`: Workspace ist `isShared`.
5. Gegenprobe: falscher Code → „Ungültiger Code".
6. Danach: Realtime-Replikation für `accounts`/`invoices`/`offers`/`payments` in Supabase verifizieren → Lead-Anlage erscheint beim anderen live.

---

## Self-Review-Notiz

- **Spec-Abdeckung:** SQL/Spalte/Backfill/RPC/RLS (Task 1) · `generateJoinCode` (Task 2) · `joinWorkspaceByCode` (Task 4) · `join_code`-Feld + Select + createWorkspace (Task 5) · `regenerateJoinCode` (Task 6) · Owner-UI (Task 7) · Member-UI (Task 8) · Fehlerbehandlung (Tasks 4/6/8 Tests) · Teststrategie (unit + component + manuelle End-to-End-Sektion). Alle Spec-Punkte abgedeckt.
- **Test-Infra:** `rpc` + `getUser` werden in Task 3 zum globalen Mock ergänzt, bevor Tasks 4/5 sie brauchen.
- **Typkonsistenz:** `join_code: string | null` (Workspace) · `joinWorkspaceByCode(code: string) => Promise<void>` · `regenerateJoinCode(workspaceId: string) => Promise<string>` · `generateJoinCode()` — durchgängig in Store, Tests und UI identisch. RPC-Name `join_workspace_by_code` + Param `p_code` in SQL (Task 1) und Aufruf (Task 4) gleich.
- **Scope:** Nur Subsystem B; Profile/Badges/Attribution bewusst draußen.
